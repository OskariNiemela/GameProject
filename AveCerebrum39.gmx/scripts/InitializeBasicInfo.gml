///InitializeBasicInfo(Owner,PassThrough,StopAbilities,CanTakeDamage);

//This script initializes all the basic info that every instance that is placed on the map needs (Passthrough,Owner,Damage etc.) Useful for non actor non AI instances.



Damage = 0;
Owner = argument0;
PassThrough = argument1;
StopAbilities = argument2;
CanTakeDamage = argument3;
